{
  "name": "merge-directories",
  "version": "1.0.0",
  "main": "merge-directory.js",
  "license": "MIT",
  "scripts": {
    "copy": "node --env-file=.env merge-directory.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
